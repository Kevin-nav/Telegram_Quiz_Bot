# Backend origin that serves /admin/* endpoints.
# Keep this aligned with ADMIN_ALLOWED_ORIGINS on the backend.
NEXT_PUBLIC_ADMIN_API_BASE_URL=http://localhost:8000

# Production note:
# Leave NEXT_PUBLIC_ADMIN_API_BASE_URL unset or empty when the admin hostname
# proxies /admin/* through the same origin at ingress level.
